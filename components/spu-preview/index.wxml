<view class="container">
    <image class="img" src="{{data.img}}"></image>
    <view class="content-container">
        <text class="title">{{data.title}}</text>
        <view>
            <view>

            </view>
        </view>
        <l-price>{{data.price}}</l-price>
        <text></text>
    </view>
</view>